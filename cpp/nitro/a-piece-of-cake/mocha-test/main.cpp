#include <iostream>
using namespace std;

int main() {

    int length;
    cin >> length;

    int numbers[length];
    cin >> numbers;

    int sequence=0;

    for (int i=0; i < length; i++) {
        if () {
            sequence += 1
        };
    };

    return 0;
}